# 이 프로젝트 완전 분해 — 학습 인덱스

> 작성일: 2026-07-26
> 대상: 이 코드를 직접 만들었지만 "왜 이렇게 되어 있는지"를 밑바닥까지 이해하고 싶은 사람.
> 방식: top-down. 큰 그림 → 파일 → 함수 → 한 줄 → 그 밑에 깔린 CS 개념.
>
> 각 절은 **[쉬움]** 과 **[전공]** 두 버전으로 나뉜다.
> - **[쉬움]**: 비유 위주. 중학생이 읽어도 그림이 그려지는 수준.
> - **[전공]**: 정확한 용어, 대안 설계, 트레이드오프, 실패 시나리오.
>
> 각 주제는 항상 **무엇을(What) → 왜(Why) → 어떻게(How)** 순으로 내려간다.

---

## 전체 지도 — 이 서비스는 무엇인가

**[쉬움]**
학교에서 종이비행기 대회를 연다고 하자. 참가자가 비행기를 만들어 오면, 선생님이 하나씩 날려보고
날아간 거리를 재서 칠판에 순위를 적는다. 이 프로젝트는 그걸 컴퓨터가 대신 하는 것이다.

- **비행기** = 참가팀이 학습시킨 AI 모델 파일
- **날려보기** = 컴퓨터 시뮬레이터(DeepRacer)에서 3바퀴 주행
- **거리 재기** = 랩타임 측정
- **칠판** = 웹 리더보드
- **선생님** = 서버 프로그램

**[전공]**
파일 업로드를 트리거로 하는 **비동기 배치 처리 파이프라인** + 그 결과를 조회하는 **읽기 전용 공개 뷰**.
전형적인 *producer-consumer* 구조이며, 큐를 별도 미들웨어(Redis/RabbitMQ/Celery) 없이
**RDBMS 테이블 하나로 구현**한 것이 이 프로젝트의 가장 특징적인 설계 결정이다.

```
[참가자 브라우저]                       [관리자 브라우저]      [구경꾼 브라우저]
      │ POST /submit (파일)                   │ 팀등록/시즌관리        │ GET /leaderboard
      ▼                                       ▼                        ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  FastAPI 웹 (Docker 컨테이너, 포트 8000)                                  │
│   main.py → SessionMiddleware → routers/{auth,submissions,leaderboard,admin}│
└───────────────┬──────────────────────────────────────────┬───────────────┘
                │ INSERT submissions(status='queued')       │ SELECT
                ▼                                           ▼
        ┌───────────────────────────────────────────────────────┐
        │  PostgreSQL (Docker 컨테이너)  ← 이게 곧 "작업 큐"      │
        └───────────────┬───────────────────────────────────────┘
                        │ 5초마다 폴링 / FOR UPDATE SKIP LOCKED
                        ▼
        ┌───────────────────────────────────────────────────────┐
        │  worker/run.py  (컨테이너 아님! WSL 호스트에서 직접 실행) │
        │   ├─ 압축 해제 → boto3로 MinIO(S3)에 모델 업로드        │
        │   ├─ bash run_evaluation.sh → dr-start-evaluation      │
        │   │        └─ Docker Swarm 스택(로보메이커 시뮬레이터)   │
        │   ├─ metrics json 파싱 → EvaluationResult INSERT        │
        │   └─ 영상 다운로드 → storage/videos/                    │
        └───────────────────────────────────────────────────────┘

  storage/  ← 웹 컨테이너와 호스트 워커가 bind mount로 공유 (경로가 서로 다름!)
```

---

## 학습 순서와 각 단계에서 얻는 것

| # | 문서 | 다루는 파일 | 이 단계가 끝나면 답할 수 있어야 하는 질문 |
|---|---|---|---|
| 1 | [01-skeleton.md](01-skeleton.md) | `main.py` `config.py` `db.py` | 브라우저 주소창에 URL을 치면 내 코드의 어느 함수가 왜 실행되는가? |
| 2 | [02-data-model.md](02-data-model.md) | `models.py` `migrations/` | "팀당 동시 제출 1건"을 왜 파이썬 `if`문만으로 막으면 안 되는가? |
| 3 | [03-auth.md](03-auth.md) | `deps.py` `security.py` `routers/auth.py` | 서버는 요청을 보낸 사람이 누구인지 어떻게 아는가? 비밀번호는 왜 복호화가 불가능한가? |
| 4 | [04-submit.md](04-submit.md) | `routers/submissions.py` `quota.py` `storage_paths.py` | 500MB 파일 업로드가 서버 메모리를 터뜨리지 않는 이유는? |
| 5 | [05-leaderboard.md](05-leaderboard.md) | `routers/leaderboard.py` `records.py` `templates/` | 팀 이름에 `<script>`를 넣어도 왜 안전한가? |
| 6 | [06-worker.md](06-worker.md) | `worker/*` | 워커가 평가 도중 죽으면 그 제출은 어떻게 되는가? |
| 7 | [07-ops.md](07-ops.md) | `Dockerfile` `docker-compose.yml` | 웹은 컨테이너, 워커는 호스트 — 이 비대칭이 왜 필요했고 어떤 장애를 낳았는가? |
| 8 | [08-crosscutting.md](08-crosscutting.md) | 전체 | 이 코드에서 "동시성", "실패", "시간"을 다루는 방식을 한 번에 정리 |

---

## 이 프로젝트를 관통하는 5개의 큰 질문

공부하는 내내 이 질문들을 머릿속에 두면 흩어진 코드가 하나로 묶인다.

### Q1. 왜 웹 서버와 워커를 분리했는가?
평가 한 건에 **10분**이 걸린다. HTTP 요청 하나를 10분 붙잡고 있으면
브라우저는 타임아웃 나고, 서버 워커 스레드는 그동안 다른 요청을 못 받는다.
→ **"오래 걸리는 일은 요청-응답 주기 밖으로 뺀다"** 는 웹 백엔드의 제1원칙.

### Q2. 왜 큐를 DB 테이블로 만들었는가?
Redis/Celery를 붙이면 운영할 프로세스가 하나 더 늘고, "DB에는 done인데 큐에는 남아있는"
**이중 진실(dual source of truth)** 문제가 생긴다. 트래픽이 초당 수천 건이 아니라
**하루 50건**이라면 DB 폴링이 압도적으로 단순하고 안전하다.

### Q3. 왜 상태(status)를 이렇게 집요하게 관리하는가?
`queued → running → done/error`. 이 4개 상태가 곧 **하루 제출 한도**, **동시 제출 제한**,
**대기열 순서**, **재시도 가능 여부**, **디스크 정리 대상**을 전부 결정한다.
상태 하나가 잘못 남으면 팀은 영원히 제출을 못 한다.

### Q4. 왜 "실패"에 대한 코드가 이렇게 많은가?
외부 도구(DRFC), 외부 저장소(MinIO), 외부 프로세스(bash), 사용자 입력(압축 파일) —
**내 통제 밖에 있는 것이 4개**다. 이런 시스템에서 코드의 절반이 에러 처리인 건 정상이다.

### Q5. 왜 "경로"에 별도 모듈(`storage_paths.py`)이 있는가?
같은 파일이 웹에서는 `/app/storage/models/1/6/x.tar.gz`,
워커에서는 `/mnt/c/Users/.../storage/models/1/6/x.tar.gz`로 보인다.
**절대 경로를 DB에 저장한 순간 시스템이 깨진다.** 실제로 2026-07-26에 장애가 났다.

---

## 공부하는 방법 (권장)

1. 문서를 읽고 → **코드 파일을 열어 대조**한다. 문서만 읽으면 남지 않는다.
2. 각 문서 끝의 **자가 점검 질문**에 소리내어 답해본다. 막히면 그 절만 다시 읽는다.
3. **실험 과제**를 실제로 해본다. 코드를 고쳐 깨뜨려 보는 것이 가장 확실하다.
   ```bash
   PYTHONPATH=. .venv/bin/python -m pytest tests -q
   ```
4. 순서를 지켜라. 1→2→3이 아니라 6번(워커)부터 보면 `models.py`를 모르는 상태라 반드시 막힌다.
